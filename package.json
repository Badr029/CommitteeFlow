{
  "name": "committeeflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CommitteeFlow — live Committee Planning & Booking System for industrial operations",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "lint": "npm run lint --workspace server && npm run lint --workspace client",
    "test": "npm run test --workspace server && npm run test --workspace client",
    "test:client": "npm run test --workspace client",
    "migrate:up": "npm run migrate:up --workspace server",
    "migrate:down": "npm run migrate:down --workspace server",
    "seed": "npm run seed --workspace server",
    "users": "npm run users --workspace server --",
    "demo": "npm run demo --workspace server --",
    "db:up": "docker compose -f docker-compose.dev.yml up -d",
    "db:down": "docker compose -f docker-compose.dev.yml down",
    "test:server": "npm run test --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
